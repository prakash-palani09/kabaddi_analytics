# Kabaddi Analytics — UI Requirements
# These are already covered by requirements.txt
# This file exists for clarity — install requirements.txt first

# Already in requirements.txt (listed here for reference):
# matplotlib>=3.7.0       ← charts, radar chart
# Pillow>=9.0.0           ← keyframe image display
# tkinter                 ← built into Python, no install needed
